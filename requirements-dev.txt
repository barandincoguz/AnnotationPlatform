-r requirements.txt
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
ruff==0.12.12
# psycopg moved to requirements.txt (Phase 6 mirror is a runtime dep);
# kept implicit via `-r requirements.txt` above.
