# Test-mode environment. Auto-loaded by Vite/Vitest when NODE_ENV=test.
# Vitest needs an absolute origin so jsdom can construct Request URLs; MSW
# handlers across the suite match against `http://localhost/...`.
VITE_API_BASE_URL=http://localhost
